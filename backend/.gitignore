node_modules/
dist/
coverage/

# Secrets
.env
.env.*
!.env.example

# Prisma generated client (regenerated by `prisma generate`)
# (kept out of VCS; CI runs prisma generate)

# Logs / OS
*.log
.DS_Store

# Migration dumps (contain source PII — never commit)
scripts/dump/
